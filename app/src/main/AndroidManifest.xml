<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kanofood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kanofood"
        android:usesCleartextTraffic="true">
        <activity android:name=".OrderCustomer"></activity>
        <activity android:name=".StoreAdmin" />
        <activity android:name=".EditUser" />
        <activity android:name=".AddUser" />
        <activity android:name=".EditFood" />
        <activity android:name=".AddStore" />
        <activity android:name=".AddFood" />
        <activity android:name=".Menu" />
        <activity android:name=".Register" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>